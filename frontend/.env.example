# Copy this file to .env.local for local development
# Do NOT commit .env.local to version control

# Local development (npm run dev — points directly to Spring Boot):
VITE_API_BASE_URL=http://localhost:8080

# Docker Compose sets VITE_API_BASE_URL=http://nginx in docker-compose.yml
# You do not need this file when running via Docker Compose